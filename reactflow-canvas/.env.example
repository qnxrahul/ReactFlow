VITE_WORKSPACE_SYNC_ENDPOINT=ws://localhost:1234
VITE_WORKSPACE_SYNC_ROOM=reactflow-workspace-boards
# Fast Agent configuration
# If not set, the app gracefully falls back to a built-in mock.

VITE_FAST_AGENT_BASE_URL=http://localhost:8000
# If using fast-agent.ai cloud, set these:
VITE_FAST_AGENT_API_KEY=
VITE_FAST_AGENT_AGENT_ID=
VITE_FAST_AGENT_RUN_PATH=
# VITE_FAST_AGENT_API_KEY=sk_live_...
# VITE_FAST_AGENT_AGENT_ID=agnt_12345
# Optional custom run path template (e.g. /api/v1/agents/:id/run)
# VITE_FAST_AGENT_RUN_PATH=/api/v1/agents/:id/run
